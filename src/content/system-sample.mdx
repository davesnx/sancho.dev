This paragraph is rendered by the same MDX pipeline as every article.

1. Ordered lists preserve sequence.
2. They keep the article spacing and marker treatment.

- Unordered lists use the same prose measure.
- Long items stay inside the content width.

> Blockquotes use a complete neutral surface rather than a decorative side stripe.

| Element | Source |
| --- | --- |
| Prose | MDX |
| Highlighting | Shiki |

---

```ocaml
let greeting = "hello"
let () = print_endline greeting
```

![Editor showing a Dune cram test](/images/cram.png)

<Note>Notes use the production semantic aside component.</Note>
